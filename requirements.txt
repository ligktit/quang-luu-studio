# Requirements cho LiveStudio
# Cài đặt: pip install -r requirements.txt

# Audio processing cho tính năng chấm điểm
librosa>=0.10.0
numpy>=1.24.0
soundfile>=0.12.0  # Cần cho librosa đọc file audio

# YouTube download
yt-dlp>=2023.10.0  # Tải audio từ YouTube
ffmpeg-python>=0.2.0  # Xử lý audio (cần cài FFmpeg riêng)

# GUI
customtkinter>=5.0.0

# MIDI
mido>=1.2.10
python-rtmidi>=1.5.0

# System automation
pyautogui>=0.9.54
pywin32>=305; sys_platform == 'win32'
psutil>=5.9.0
uiautomation>=2.0.0

# System audio capture (loopback recording)
soundcard>=0.4.2

# Get OS Media Controls (Browser media status)
winrt-Windows.Media.Control>=3.2.1
winrt-Windows.Foundation>=3.2.1