# === Quang Lưu Studio — Dependencies ===

# UI Framework (PySide6, NOT PyQt5)
PySide6>=6.5

# MIDI
mido>=1.3
python-rtmidi>=1.5

# Audio
pyaudiowpatch>=0.2.12
sounddevice>=0.4
# librosa 1.0 xoá librosa.beat.tempo và đổi API — pin <1.0 cho tới khi migrate xong
librosa>=0.10,<1.0
numpy>=1.24
scipy>=1.10
# Điều khiển âm lượng thiết bị Windows (core/engine/_recording.py)
pycaw>=20240210

# YouTube
yt-dlp>=2024.1
# CDP WebSocket client (core/cdp_monitor.py)
websocket-client>=1.6
# Tự tải ffmpeg cho detect_youtube.py (tuỳ chọn — có fallback nếu thiếu)
static_ffmpeg>=2.5

# Windows APIs
pywin32>=306
pyautogui>=0.9
psutil>=5.9
uiautomation>=2.0

# Windows Media Control (WinRT)
winrt-Windows.Media.Control>=2.0
winrt-Windows.Foundation>=2.0

# Accessibility — TTS (SAPI5 offline) + Voice command (Vosk offline VI)
# pyttsx3 bắt buộc cho TTS. vosk là tuỳ chọn — voice command sẽ tắt nếu chưa cài
# hoặc chưa tải model vào models/vosk-vi/.
pyttsx3>=2.90
vosk>=0.3.45