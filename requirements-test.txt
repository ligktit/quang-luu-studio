# Test-only dependencies — subset of requirements.txt (no hardware/audio devices needed)
# Dùng trong CI: pip install -r requirements-test.txt

# Core logic
numpy>=1.24
scipy>=1.10
librosa>=0.10

# MIDI (dùng mock trong tests, nhưng cần import)
mido>=1.3
python-rtmidi>=1.5

# YouTube
yt-dlp>=2026.7.4

# System
psutil>=5.9
pywin32>=306

# UI (một số tests cần Qt)
PySide6>=6.5

# Test framework
pytest>=7.0
pytest-mock>=3.12
pytest-cov>=4.0
pytest-qt>=4.3
