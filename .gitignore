# Sensitive — activation codes & keys
docs/ACTIVATION_CODES.md
activation_codes.txt

# Build outputs
# LƯU Ý: installer_output/ chỉ chứa file Setup_*.exe do Inno Setup xuất ra.
# TUYỆT ĐỐI không copy source code / thư mục project vào đây (đã từng xảy ra
# 30-04-2026: cả project bị copy thành installer_output/quang-luu-studio/,
# kèm .git và .venv riêng — 107 MB rác và gây nhầm lẫn cấu trúc thư mục).
dist/
dist_heavy/
build/
installer_output/
*.spec
!QuangLuuStudio.spec

# PyInstaller spec cache
*.spec.cache

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
*.egg
.eggs/

# Virtual environments
venv/
.venv/
env/
.env/

# Environment variables
.env
*.env.local

# IDE
.vscode/
.idea/
*.suo
*.user

# Logs
*.log
log.txt

# OS
.DS_Store
Thumbs.db

# Temp audio files
temp_audio/

# Test / batch outputs
batch_output.txt
test_output.txt

# Binary tools
ffmpeg.exe
*.exe

# Large media / assets
*.png
*.ico
!app_icon.png
!app_icon.ico
sfx/

# Data / cache (user-specific)
activation.json
saved_songs.json
playlists.json
settings.json
tone_cache.json
manual_timelines.json
ui_config.json
accessibility_overrides.json
calibration_overrides.json
crash_queue.json
support_queue.json
tone_share_queue.json
activation_codes.txt
cdp_profile/

# Backup files (config snapshots)
*.bak

# Spec file (generated by PyInstaller)
*.spec
!QuangLuuStudio.spec

# Test / log artifacts
.pytest_cache/
logs/
@AutomationLog.txt

# Claude local settings (machine-specific)
.claude/settings.local.json

# Git worktrees
.claude/worktrees/

# Voice models lớn (tải qua tools/download_voice_models.py — không commit binary nặng)
models/vosk-vi-large/
models/piper-vi/
tools/piper/

# Binary phụ trợ cho YouTube — tải lúc build/lúc chạy, không commit
# (xem core/pot_provider.py, core/utils.py::find_js_runtime, build_installer*.bat)
binaries/
pot/
pot_provider.json
ytdlp/
ytdlp_update.json
qjs.exe
ffmpeg/
